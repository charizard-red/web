import React from "react";

import Header from "./Header";

const PageControl = props => {
  return (
    <div>
      <Header />
      {props.children}
      {Any Web Page}
      {Any Web Page}

    </div>
  );
};

export default PageControl;
